@page
@using Net.Leksi.Pocota.Test.RandomPocoUniverse;
@model ClassModel
@foreach (string usng in Model.Usings.OrderBy(v => v))
{
    <text>using @usng;
</text>
}
@if (Model.Node.Namespace is { })
{
    <text>namespace @Model.Node.Namespace;
</text>
}

public abstract class @Model.Node.Name@if (Model.Class is 
{ }) { <text>: @Html.Raw(Model.Class)</text> }

{
@foreach(PropertyDescriptor pd in Model.Node.Properties)
{
    <text>    public abstract @Html.Raw(pd.TypeString) @pd.Name { get; @if(!pd.IsReadOnly){<text>set;</text>}}
</text>
}
}
