@page
@using Net.Leksi.Pocota.Test.RandomPocoUniverse;
@model InheritModel

@foreach (string usng in Model.Usings.OrderBy(v => v))
{
    <text>using @usng;
</text>
}

@if (Model.Holder.Namespace is { })
{
    <text>namespace @Model.Holder.Namespace;
</text>
}

public class @Model.Holder.ClassName: @Html.Raw(string.Join(", ", Model.Holder.Base))