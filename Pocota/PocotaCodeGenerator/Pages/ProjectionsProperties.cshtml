@using Net.Leksi.Pocota.Common;
@model ClassModel

@foreach(ClassModel model1 in Model.Classes)
{
    <text>    private @model1.ClassName? @Html.Raw($"_as{model1.ClassName}") = null;
</text>
}

@foreach(ClassModel model1 in Model.Classes)
{
    <text>    public @model1.ClassName @Html.Raw($"As{model1.ClassName}") => @Html.Raw($"_as{model1.ClassName}") ??= new(this);
</text>
}

